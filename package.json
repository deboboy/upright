{
  "name": "upright",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "serve": "python3 -m http.server 5173 --directory web",
    "check": "node --check web/app.js && node --check web/bridge-sdk.js && node --check web/posture-core.js && node --check web/session-store.js && node --check web/mock-bridge.js"
  }
}
